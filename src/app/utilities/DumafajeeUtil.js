var $ = require('jquery');

module.exports = {

  getAllDumafajees: function() {
    // simulate retrieving data from a database
    //var rawMessages = JSON.parse(localStorage.getItem('messages'));

    // simulate success callback
    //ChatServerActionCreators.receiveAll(rawMessages);
  }
};